/** Card title: Plan usage limits, Project spend, Last month window. */
export const usageCardTitle: Record<string, string> = {
  "font-size": "18px",
  "font-weight": "700",
  "letter-spacing": "-0.01em",
  color: "var(--text-1)",
};

/** Date range line under the card title. */
export const usageRangeCaption: Record<string, string> = {
  "font-size": "12px",
  color: "var(--text-2)",
  "line-height": "1.35",
  "letter-spacing": "-0.01em",
  "margin-top": "2px",
};

/** Explanatory helper copy (empty state, hints). */
export const usageHelperText: Record<string, string> = {
  "font-size": "11px",
  color: "var(--text-2)",
  "line-height": "1.4",
};
